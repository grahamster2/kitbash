{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window. Server access is via this crate's own commands, so no HTTP-scope permissions are needed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
